//meta game data

export default class Meta {

    constructor(){
        this.isStarted = false;
        this.isFirstRound = true; //for things initialized on first run of loop, disabled thereafter 
    }
}